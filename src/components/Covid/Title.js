import React from 'react'

const Title = (props) => {
    return (
        <h1 className='font-poppins text-slate-700 text-xl font-bold tracking-wide text-center'>{props.Title}</h1>
    )
}

export default Title